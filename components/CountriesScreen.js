import { View, Text } from "react-native";

export const CountriesScreen = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Countries Screen ABC</Text>
    </View>
  );
};

export default CountriesScreen;
